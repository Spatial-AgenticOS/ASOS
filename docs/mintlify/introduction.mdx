---
title: "FERAL Overview"
sidebarTitle: "Introduction"
description: "What FERAL is, what it controls today, and how to get started without guesswork."
---

<img src="/images/feral-banner.png" alt="FERAL" />

# What FERAL is

FERAL is a local runtime that coordinates AI decisions across your software and physical devices.

- Brain runtime: tool orchestration, memory, policies, and device/session routing.
- Web UI: setup, pairing, chat, SDUI surfaces, and operational visibility.
- Node/device paths: phone/browser nodes and daemon bridges for hardware control.

The product is strongest when you treat it as a supervisor layer over existing systems (LLM providers, channels, automation targets, and hardware adapters), not as a cloud replacement SaaS.

## What it can do now

- Coordinate actions across chat, tools, channels, and connected devices from one runtime.
- Keep persistent memory and state in your local FERAL home.
- Pair phones and browser nodes over LAN or remote tunnel modes.
- Render server-driven UI (Gen-UI/SDUI) as structured components in the web client.
- Enforce explicit action paths (including confirmation gates where configured).

## Connectivity model

FERAL has three pairing/access modes:

- **Same WiFi** (`local`): pair from phone when both devices share a network.
- **Anywhere** (`remote`): pair/use through Tailscale Funnel.
- **This Mac only** (`localhost`): no phone pairing yet.

See [Quickstart](/quickstart) for first run and [Pairing & Access](/guides/pairing-access) for exact LAN vs Anywhere steps.

## Gen-UI in one paragraph

Gen-UI is server-driven UI. The brain emits structured UI payloads and patches; the client renders supported components. This is controlled, contract-based rendering, not arbitrary HTML generation.

## Start here

<CardGroup cols={2}>
  <Card title="Quickstart" icon="play" href="/quickstart">
    PyPI-first install, first run, and first pairing flow.
  </Card>
  <Card title="Pairing & Access" icon="wifi" href="/guides/pairing-access">
    Same WiFi vs Anywhere, with exact commands and failure recovery.
  </Card>
  <Card title="Gen-UI Guide" icon="sparkles" href="/guides/genui">
    What SDUI does in this repo and where the current limits are.
  </Card>
  <Card title="Architecture" icon="sitemap" href="/architecture">
    Runtime components and how requests flow through the brain.
  </Card>
</CardGroup>
